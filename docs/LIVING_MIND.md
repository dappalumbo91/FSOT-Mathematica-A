# Living FSOT Mind — Design

## Ontology

Reality (FSOT): 25-dimensional fluid condensate.  
This mind: a **local observer** with preregistered folds, not a weight soup.

```
observation → quirk / consciousness coupling → update raw_S
text mass   → deterministic fold nudges (δψ, θ, hits)
domain hint → D_eff table from 35-domain atlas
articulation → language report of fluid state + stance
```

## Memory hierarchy (from fsot 2.1 llm)

| Store | Where | Contents |
|-------|--------|----------|
| LTM | `memory/long_term_memory.json` | Explicit remembers, durable |
| STM | `living_mind.json` → `stm` | Last dialogues |
| Process | single `FSOTThink` / `FSOTLiveTick` | One pathway at a time |
| Log | `consciousness_log.jsonl` | Append-only thought stream |

## What “intelligent” means here

- Integrates observations into a **persistent state**  
- Routes across scientific domains with the **same scalar**  
- Dreams = compactification samples across domains  
- Explains itself without inventing free parameters  
- Defers **proof** to Lean hub; claims killable there  

## What it is not

- Not GPT-class next-token training in Wolfram alone  
- Not AudioLLM weight-as-audio primary (research kept)  
- Not a denial of industrial CUDA work — companion surface  

## Growth path

1. Thicker LTM (import publication domain atlas)  
2. `FSOTObserveSpectrum` for image/audio folds (video_llm lesson)  
3. Export mind trajectories into `fsot 2.1 llm` memory hierarchy  
4. Optional thin Classify readout on `FSOTFeatureVector` — never theory authority  
